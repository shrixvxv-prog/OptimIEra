# Environment variables

Variable names are documented in the repository `.env.example` and the reference page.

Phase 2 local development requires database and auth settings plus `OPTIMIERA_ENCRYPTION_MASTER_KEY` for encrypted prompt and candidate storage. 0G, Agentic ID, payment, and production deployment values remain unconfigured until later phases.
