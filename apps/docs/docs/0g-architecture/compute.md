# 0G Compute architecture

Purpose: model-assisted prompt analysis and candidate generation through the official Router. Status: **IMPLEMENTED**; live status is **UNCONFIGURED** without credentials. Sources: https://docs.0g.ai/developer-hub/building-on-0g/compute-network/router/overview and https://docs.0g.ai/developer-hub/building-on-0g/compute-network/router/quickstart · Verified: 2026-07-16. Package: `@optimiera/og-compute`. Environment: `OG_COMPUTE_ENABLED`, `OG_COMPUTE_NETWORK`, `OG_COMPUTE_BASE_URL`, `OG_COMPUTE_API_KEY`, `OG_COMPUTE_MODEL`, `OG_COMPUTE_TIMEOUT_MS`, `OG_COMPUTE_MAX_OUTPUT_TOKENS`. Files: `packages/og-compute/src/index.ts`, `scripts/og-compute-check.mjs`. Data flow: local analyzer → minimized server-side Router request → strict schema validation → local safety/evaluation → encrypted persistence. Errors: safe HTTP mapping, one repair attempt, bounded 429/502 retry, timeout. Security: `sk-` credentials remain server-side; prompts and secrets are not logged. Evidence: public testnet model discovery succeeded; authenticated inference is not claimed. Limitation: no live inference until a configured, funded Router key and model are supplied.
