export default function Product() {
  return (
    <main className="site">
      <section className="section">
        <div className="eyebrow">OptimIEra Studio</div>
        <h1>One workspace for prompt reliability.</h1>
        <p className="lede">
          Analyzer, optimizer, diff viewer, Evaluation Lab, Registry, certificates, teams, APIs,
          SDK, CLI, monitoring, marketplace, Agent, and Proof Center are architected as a cohesive
          product system.
        </p>
        <a className="button primary" href="/app">
          Open the Studio shell
        </a>
      </section>
    </main>
  );
}
