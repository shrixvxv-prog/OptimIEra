export default function VerifyEmail() {
  return (
    <main className="hero">
      <div className="eyebrow">Account security</div>
      <h1>Verify your email.</h1>
      <p className="lede">
        Email verification is configured. Local development uses a redacted server notification;
        production delivery must be configured before launch.
      </p>
    </main>
  );
}
