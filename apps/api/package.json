{
  "name": "@optimiera/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@optimiera/database": "workspace:*"
  },
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
