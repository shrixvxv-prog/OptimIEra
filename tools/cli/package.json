{
  "name": "@optimiera/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "optimiera": "src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json"
  }
}
