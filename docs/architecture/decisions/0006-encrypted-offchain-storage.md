# ADR 0006: Encrypted off-chain prompt evidence

Status: accepted. Private artifacts must be encrypted before decentralized storage. This prevents public storage from becoming a plaintext prompt repository. Implementation is present in `@optimiera/og-storage` with local encrypted evidence, canonical manifests, and proof-verification tests.
