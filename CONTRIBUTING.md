# Contributing to OptimIEra

Use Windows-compatible PowerShell commands, keep TypeScript strict, and preserve explicit capability status labels. Do not add secrets, fake deployment evidence, or unverified integration claims. Before a pull request, run `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm docs:build`, and `pnpm build`.
